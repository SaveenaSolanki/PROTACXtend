# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Data (large, cloned repos, models — never commit)
data/protac_repos/repos/
data/benchmark/_ad_cache/
outputs/benchmark/chemprop_*/
outputs/benchmark/*.log
outputs/p4ward_evidence/*.pse
outputs/p4ward_evidence/*.tar.gz
outputs/runs/
outputs/coverage/
protacxtend/outputs/candidates/*.csv
protacxtend/outputs/candidates/*.json
protacxtend/outputs/reports/*.md
protacxtend/memory/workflow_logs/*.json
protacxtend/memory/learnings/runs/

# Notebooks / checkpoints
.ipynb_checkpoints/
*.checkpoint

# Env / secrets
.env
*.key
nohup.out

# OS
.DS_Store

# --- v0.3 controlled GitHub hygiene (added 2026-08-07) ---
# Virtualenvs / runtime dirs (never commit)
.venvs/
data/synthesis_prediction/envs/
data/synthesis_prediction/install_logs/

# Cloned upstream dependency repos + generated outputs

# Large data/model binaries (re-downloadable; see README/DOCS)
data/retrosynthesis/models/
*.hdf5
*.pkl
*.pt
*.onnx
*.sdf
*.pptx
*.zip
# Runtime state DBs + cloned synthesis repos
*.sqlite3
*.sqlite
data/synthesis_prediction/repos/

# Logs (secrets can hide here)
*.log

# Machine-local paths (absolute) — never commit
env/protacpilot_config.json

# deep-research framework caches / traces (runtime, regenerable)
data/research/cache/
outputs/research_traces/
outputs/omics_cache/
SynGlue_Py/
artifacts/
docs/
protacxtend/outputs/
protacxtend/memory/workflow_logs/
protacxtend/memory/learnings/runs/
protacxtend/memory/agentic_design_memory.jsonl
